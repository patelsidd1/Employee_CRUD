spring.profiles.active=${ACTIVE_PROFILE}
spring.application.name=Employee

spring.datasource.url=jdbc:mysql://localhost:3306/config_employee?createDatabaseIfNotExist=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=abcd@123

#Automatic create the Database
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Change this to qa, uat, preprod, prod
#spring.profiles.active=dev

# Default is dev if not set



